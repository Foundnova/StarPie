# StarPie 插件化改造大纲（PLUGIN_FIRST_ROADMAP）

> 目标：评估「一切皆插件」在 StarPie 上的可执行边界，给出分类清单、核心保留项、划分原则与风险控制。
> 前置事实：插件系统的**地基已经落地**（`StarPie.Plugin.Abstractions` SDK + 宿主 ALC 加载/卸载 + 只读来源区/可写宿主区目录模型 + 声明式参数表单 + CLI 全链路自检）。
> 本文讨论的是**往上盖楼**，不是重新论证地基。

---

## 0. 结论先行

### 0.1 「一切皆插件」不能按字面执行

它和项目三条工程红线**直接冲突**，且冲突点是可以逐条列出来的：

| 红线 | 与「一切皆插件」的冲突 | 量化边界 |
|---|---|---|
| 轻量低内存 | 每个常驻插件 = 1 个可回收 ALC + 1 个实例 + 独立的词条/图标/参数注册表；插件数量从 0 变 N 时内存单调上升 | 后台驻留基线 15~30MB 是**没有插件**时的数字，必须重新建立「每插件常驻增量」的实测基线后再定上限 |
| 零延迟丝滑 | 只要渲染主干或命中判定上出现一处插件代码，16ms 预算里就多了一项**无法静态保证**的耗时（插件可以在回调里做任何事） | 轮盘呈现 <16ms 是硬指标；插件代码不得出现在「按下 → 呈现」这条链上 |
| 肌肉记忆确定性 | 装/卸插件会改变动作类型下拉的条目、改变扇区实际行为；用户的扇区配置会因为「别人装了什么」而语义漂移 | 扇区角度与既有配置的语义**不得**随插件集合变化 |

**结论**：字面意义的「一切皆插件」= 主动放弃三条红线。不建议。

### 0.2 可执行的目标形态：核心稳定 + 边缘全开放

把「一切皆插件」翻译成能落地的一句话：

> **热路径与长期契约沉到核心并冻结；热路径两侧的一切（动作、数据源、UI 描述、集成适配）全部开放为插件贡献点。**

这不是妥协——它拿到「一切皆插件」90% 的实际收益（扩展性、社区贡献、解耦），代价却可控。

### 0.3 一句话判据

三个问题中**任一**答「是」，则不插件化：

1. 它是否位于**「用户操作 → 屏幕反馈」的 16ms 热路径**上？
2. 它是否是**跨版本长期存在的数据契约**（配置文件字段、Tag 编码、注册表键）？
3. 它是否是**别的机制赖以启动的前提**（自举）？

---

## 1. 分层模型

```
L0 内核内核（Kernel）    —— 绝不动，无插件也完整运行
   钩子 / 命中判定 / 渲染主干 / SendInput 内核 / 配置持久化 / 插件加载器

L1 核心能力（Core）      —— 由核心实现，但以「贡献点」形式暴露给 L2
   动作调用接缝 / 宿主服务 / 主题画刷 / 词条解析 / 图标解析 / 安全模式

L2 可插件化（Extensible）—— 本大纲的主要讨论对象，用插件替换或增补
   具体动作 / 数据源与检索 / 集成适配 / 面板与编辑器 / 渲染策略

L3 纯插件（Plugin-Only） —— 只在插件里存在，核心完全不知道
   社区扩展 / 实验性功能 / 个人工作流定制
```

**依赖方向恒为 L3 → L2 → L1 → L0，且 L0/L1 不知道 L2/L3 的存在**（只通过 SDK 契约反向调用）。任何一层的反向依赖都是设计事故。

**准入判据**：一个功能归到哪层，看它「去掉之后核心还能不能开」。能开 → 至少 L2；开不了 → L0/L1。

---

## 2. 可插件化功能的分类清单

### 2.1 动作与业务逻辑（已部分落地，建议全面铺开）

| 模块 | 现状 | 插件化形态 | 收益 | 代价 / 约束 |
|---|---|---|---|---|
| 具体动作实现 | ✅ 已开放（`IActionRegistry`） | 插件注册 `ActionDescriptor` | 社区贡献动作 = 项目最大增长杠杆 | 必须走 `PluginInvoker` 统一调度（超时/取消/串行化），不得绕过 |
| 动作的取消/回退语义 | ⚠️ 部分 | 插件声明是否可取消 | 长耗时动作可被打断 | 需要「已开始但未完成」的状态表示 |
| 动作前置条件 | ❌ | 插件声明 `CanExecute` | 状态相关动作（如「仅当前台是 CAD 时」） | 判定必须在**非热路径**上预先求值并缓存，不能拖慢呈现 |
| 复合动作 / 宏 | ❌ | 插件组合既有动作 | 用户工作流自动化 | 宏展开在热路径上执行 → 必须有展开预算与超时 |

**判断**：这一域是插件化收益最高、风险最低的区域，应作为主力方向。

### 2.2 UI 组件

| 模块 | 建议 | 理由 |
|---|---|---|
| 参数表单 | ✅ 已由宿主渲染（声明式） | **这是正确范式，应推广到全部 UI**。插件只交描述，宿主负责渲染，天然获得主题/DPI/缩放着色一致性 |
| 设置页内的插件专属面板 | ⚠️ 有限开放 | 只允许「描述式」面板（表单 + 列表 + 文本），不允许插件自带 `Window` |
| 插件自带独立窗口 | ❌ 不建议 | 破坏视觉一致性、主题注入、80%~200% 缩放适配、UIPI 防护；且插件窗口崩溃会带崩宿主进程 |
| 渲染策略（`IRadialStyleRenderer` 族） | ✅ 高价值 | 现有 `ClassicRing` / `CleanSectors` / `Glassmorphism` 已是策略模式，天然是贡献点 |
| 托盘子菜单项 | ✅ 可开放 | 低频交互，无热路径风险 |
| 设置窗页面（NavTab） | ❌ 不开放 | 导航结构属于肌肉记忆的一部分；插件页面会改变页序与数量 |

**关键原则**：**UI 的「描述」可以来自插件，UI 的「渲染」永远由宿主完成。**
这与现有声明式参数表单是同一个思路，直接沿用即可，不需要新发明。

### 2.3 数据处理

| 模块 | 建议 | 理由 |
|---|---|---|
| 配置导入 / 导出格式适配 | ✅ 适合 | 格式转换是纯函数式工作，失败域清晰 |
| 配置迁移脚本 | ⚠️ 有限开放 | 迁移必须**确定性、幂等、可回滚**；插件化会让「升级路径」依赖插件存在与否，风险高。建议：核心定框架，插件的迁移只有在插件已安装时生效，且失败必须回落 |
| 搜索索引 / 排序策略 | ✅ 适合 | 如「软件检索器」的匹配排序算法可由插件替换 |
| 日志聚合与分析 | ✅ 适合 | 完全离线、非热路径 |
| 数据校验规则 | ✅ 适合 | 与现有 `PluginParameterValidator` 两层校验同构，扩展即可 |
| 配置文件的**序列化格式本身** | ❌ 核保留 | 见 §3.6 |

### 2.4 第三方集成

| 集成对象 | 建议 | 理由 |
|---|---|---|
| 全盘秒搜（Everything） | ✅ 高价值 | 已经是「外部依赖 + 权限受阻感知」的典型，插件化后可由用户选择不同后端 |
| OCR 引擎 | ✅ 高价值 | 引擎可替换（不同精度/速度/离线方案），核心只保留「截屏 → 取结果 → 呈现」流程 |
| 更新检查通道 | ❌ 核保留 | 自更新涉及任意代码执行与信任边界，不能由第三方插件接管 |
| 云同步 / 账号 | ⚠️ 谨慎 | 涉及凭据与隐私，需要独立的能力门禁与显式授权；建议先做成 L1 能力再考虑插件适配 |
| 音效引擎 | ✅ 适合 | `CustomSoundProfile` 已是数据驱动，后端可替换 |
| 主题 / 配色方案 | ✅ 适合 | 纯数据 + 画刷，但必须**强制 `Freezable.Freeze()`** 并走宿主的主题注入通道 |
| 剪贴板 / 通知 / 文件操作 | ⚠️ 只作为**宿主服务**开放 | 插件不得自己 `Process.Start` 或直接 Win32 调用绕过宿主——这是既有约定，要守住 |

### 2.5 视觉表现

- 渲染器策略族（已具备）：新增形态（如渐隐、毛玻璃变体）走插件，核心只保留「几何切削基类 + 工厂 + 命中判定」。
- **但**：渲染器必须运行在宿主的渲染管线内，遵守既有画刷冻结与缓存约定；插件不得自建 `Window`、不得引入额外渲染线程。

### 2.6 支撑能力（建议最后考虑）

- 命令行子命令（诊断、导出、批量操作）：可开放为「插件注册 CLI 子命令」，但**基础通道（`--plugin-selftest` / `--plugin-paths`）必须留在核心**，否则插件坏了就没有诊断手段。
- 快捷键/手势的**绑定策略**：可插件化；但**绑定的采集与命中**在 L0。

---

## 3. 必须保留在核心、不宜插件化的功能

### 3.1 启动引导与生命周期

**清单**：单例互斥锁、`App` 启动序列、窗口延迟创建与释放、托盘生命周期、退出清理、自启动注册表。

**为什么**：
- 插件加载**发生在启动序列内部**，它不可能是启动序列的消费者；
- 退出清理必须是**确定性**的：如果「是否能让插件卸载」取决于插件自己，就会出现「退出后进程残留」这类用户无法理解的故障；
- 启动路径的长度直接决定冷启动体感，不能因插件数量而变长。

### 3.2 插件加载机制本身（自举问题）

**清单**：`PluginHost`、`PluginLoadContext`（ALC）、`PluginCatalog`（贡献点注册与会话）、`PluginScanner`（静态 PE 识别）、`PluginPaths`（**路径的单一出口**）、`PluginRegistryStore`（登记与哈希）、`PluginLogger`、`PluginManifestReader`。

**为什么**：
- **自举悖论**：加载器不能由被加载物提供。「从哪读插件」「怎么判断它是插件」「加载失败怎么办」这三件事的答案必须在插件系统之外；
- `PluginPaths` 尤其关键：一旦允许第二处 `Path.Combine` 拼插件路径，只读来源区/可写宿主区模型就会被绕过，出现「往程序目录写文件」的回归；
- 加载器的失败必须**可诊断**：核心保留 `--plugin-paths` / `--plugin-selftest` 是「插件全坏了也能查」的最后一根绳。

### 3.3 热路径（最硬的一条）

**清单**：`MouseHook` / `KeyboardHook`（`WH_MOUSE_LL` / `WH_KEYBOARD_LL`）、`GestureController`（状态机、极坐标、命中测试）、`RadialWindow` 渲染主干、`ActionExecutor` 的 `SendInput` 内核（扫描码映射、修饰键时延）、`FullScreenHelper`。

**为什么**：
- 低级钩子有**系统级超时**（回调超时会被 Windows 摘掉钩子），插件代码一旦进来，稳定性不再由我们掌握；
- 命中判定是肌肉记忆的物理实现（极坐标夹角最近邻），它必须**逐字节确定**；
- 「按下 → 呈现 <16ms」是可测量承诺，插件是这条承诺上唯一无法静态分析的变量。

> 注意：这些模块的**配置与策略**（用哪个渲染器、哪个布局算法、哪些键位映射）可以来自插件；**执行这些策略的代码**必须在内核。

### 3.4 核心通信总线

**清单**：动作调用接缝 `PluginInvoker` 的调度/超时/取消/串行化、宿主服务实现（`PluginHostServices`）、事件分发、能力门禁（`PluginContext`）。

**为什么**：
- 这是**唯一**允许跨越 ALC 边界的通道。让它可插件化 = 允许插件重写调度策略 = 超时与串行化保证消失；
- 宿主服务是**权限的收口点**（插件不直接 `Process.Start`，而是请求宿主执行）。收口点被插件替换，安全模型即失效；
- 事件分发顺序若可替换，「插件 A 收到事件在先」这类行为就不可复现，调试成本爆炸。

### 3.5 基础安全与配置

**清单**：`ConfigManager`（原子写、`.bak`、损坏现场保留、回落标志）、安全模式与启动健康记账、注册表自启管理、UIPI 防护、路径解析（`PluginPaths`）、许可证与信任边界。

**为什么**：
- **配置是"最后的真相"**：如果配置读写由插件定义，一个坏插件就能让用户配置永久损坏——而这恰恰是「静默失效」最容易发生的地方，也是本项目明确最不能接受的一类缺陷；
- 安全模式的判定必须由宿主独立完成：让插件参与「谁该被禁用」的决策，等于把保险丝交给被保护对象；
- 落盘策略（原子写 + 保留现场）是长期演进的护栏，不能有第二份实现。

### 3.6 数据契约与身份

**清单**：`AppConfig` / `WheelProfile` 的序列化字段、动作的 `Tag` 编码（`PluginActionBinding` 的「Type + PluginActionRef ⇄ 单 Tag」投影）、`registry.json` / `health.json` 结构、`plugin.json` 清单 Schema。

**为什么**：
- 这些字段是**跨版本长期契约**。用户可能带着 3 年前的配置升级到最新版；契约一旦交给插件演进，就会出现「装了 X 插件才读得懂我的配置」这种不可接受的依赖；
- **失效引用的表示必须在核心**：插件被卸载后，用户扇区里指向它的引用不能变成「未设置」（那会静默改变用户行为），必须保留为显式的「引用已失效」状态。这是**核心的数据表示，不是插件的自由度**。

---

## 4. 边界划分原则（可操作判据）

按优先级排列，逐条都是**可用来吵架的**判据：

1. **热路径禁入原则**
   凡在「用户操作 → 屏幕反馈」链路内、且预算 <16ms 的代码，一律在核心。
   例外仅限：纯数据的策略选择（枚举值、配置项），且选择结果必须在呈现前**已经算好**。

2. **自举不可插件化原则**
   任何「其他东西赖以启动」的机制（加载器、日志、路径解析、配置读取）不得由插件提供。

3. **契约单向原则**
   核心**只知道 SDK**，永远不知道具体插件；插件**只引用 SDK**，永不引用宿主主程序。
   出现反向依赖即是设计事故（不只是违规）。

4. **失败域隔离原则**
   每个可插件化的点，先回答：「它失败时，用户会看到什么？」
   答案必须是「该功能不可用 + 明确文案」，而不是「主界面打不开」「配置损坏」「进程残留」。

5. **描述与渲染分离原则**
   插件的输出是**描述**（数据、清单、Schema），宿主的职责是**渲染与执行**。
   这条是 UI 类插件化的总闸门。

6. **热插拔不改变既有语义原则**
   装/卸任意插件，都不得改变用户已有轮盘方案、扇区角度、动作类型顺序的**语义**。
   允许的变化仅限于「多出一个可选项」。

7. **可归因原则（性能）**
   每个插件的资源占用（内存、加载耗时、动作耗时）必须能被**单独测量**。
   做不到归因的插件化设计应当被驳回——否则红线失守时找不到责任人。

8. **归零可运行原则**
   卸载全部插件后，产品必须是**完整可用**的，而不是「能启动但缺胳膊少腿」。
   任何违反此条的功能划分，说明它本该在 L1。

9. **能力显式声明原则**
   插件要用什么能力（文件、网络、通知、剪贴板）必须在清单里声明，宿主按声明放行。
   未声明的调用一律拒绝并留痕——拒绝要出声，不能静默失败。

10. **增量优先原则**
    优先把「新增功能」做成插件，而不是把「既有核心功能」拆成插件。
    拆既有功能只在一种情况下值得：它已经被证明是变更最频繁或分支最多的部分。

---

## 5. 潜在风险

按严重度排序。每条给：**症状 → 后果 → 护栏**。

### P0 — 会导致产品承诺失效

**R1. 红线被渐进侵蚀（温吞水效应）**
- 症状：单次改动都在预算内，累积后后台驻留从 20MB 涨到 60MB；轮盘呈现从 8ms 涨到 20ms。
- 后果：三条红线是**累积指标**，任何单次评审都看不出问题，等发现时架构已定型。
- 护栏：把「无插件基线」与「N 插件装态」都纳入可测量指标；每次引入新插件贡献点必须同时给出「该点的常驻开销」与「热路径影响（应为 0）」的实测数据；建立**插件的资源占用上限**并在超限时拒绝加载（不是警告）。

**R2. 肌肉记忆被插件集合改变**
- 症状：装了 A 插件后，动作类型下拉多了一项，用户按位置/习惯选到的类型变了；卸载插件后扇区显示「未设置」而非「引用已失效」。
- 后果：这是三条红线里最不可逆的一条——用户的手会记住错的位置。
- 护栏：新增贡献点一律**追加到末尾**，绝不插入既有顺序（现有插件动作类型已遵守此约定，需固化为规范）；失效引用必须是显式状态；贡献点的**可见性可以随插件变化，但既有项的相对顺序不能变**。

**R3. 卸载/升级路径不可控**
- 症状：插件卸载后残留 ALC 引用导致目录删不掉；升级后旧插件的 `data/` 与新版 Schema 不兼容。
- 后果：用户遇到「卸载了但还在」「装了新版起不来」，且没有自助恢复手段。
- 护栏：卸载必须验证「插件目录能被即时删除」（现有自检已把这条作为硬断言）；插件私有数据必须有版本号与迁移约定；**升级失败必须能回落到「禁用该插件但核心可用」**。

### P1 — 会导致维护成本失控

**R4. 静默失效**
- 症状：插件加载成功但动作不生效、词条没命中、图标不显示——界面一切正常，行为悄悄退化。
- 后果：本项目明确最不能接受的一类缺陷（对用户不可见、对开发者难复现）。
- 护栏：保留并扩展既有自检的**词条命中率**、**参数校验断言**、**「装完必须真的处于运行态」**等断言；每个新贡献点上线时必须配套一条自检断言，否则不予合入。

**R5. ALC 与类型身份地狱**
- 症状：`is IStarPiePlugin` 静默返回 null、强转失败、卸载不掉、加载两份同名程序集。
- 后果：极难定位（编译全过、运行时不报错）。
- 护栏：现有的 `Load()` 四步判定与 SDK 程序集唯一性约束必须作为**不可协商的契约**写进 SDK 文档；新增贡献点不得引入新的跨 ALC 类型传递方式。

**R6. SDK 契约演进的兼容地狱**
- 症状：插件按 v1 接口写的，宿主升到 v2 后加载失败或行为异常；宿主不敢改接口，SDK 冻结成化石。
- 后果：要么破坏插件生态，要么核心被 SDK 拖住无法演进。
- 护栏：`PluginApi.ApiVersion` 必须有明确的兼容策略（新增可选成员优先于修改/删除；重大变更走新接口 + 并存期）；**SDK 的二进制兼容性要像配置文件一样被当作长期契约对待**。

**R7. 调试与可观测性恶化**
- 症状：异常堆栈跨 ALC 后信息缺失；插件卡住时表现为「整个轮盘不响应」。
- 后果：用户报障时无法自助定位，开发者只能靠猜。
- 护栏：插件异常必须带插件身份落独立日志（现有 `PluginLogger` 按插件分文件，需保持）；插件动作必须有独立的耗时与超时记录；核心日志里必须能看出「这次卡顿是哪个插件造成的」。

### P2 — 会影响体验与生态

**R8. 供应链安全**
- 症状：插件 = 任意代码执行；无签名、无来源校验。
- 后果：用户从第三方站点下载插件，等同运行未知程序。
- 护栏：当前信任边界是「用户手动安装」——这个前提必须**显式告知用户**；签名校验与来源可信度属后续里程碑；但「能力显式声明 + 未声明即拒绝」应先落地，它是后续签名机制的载体。

**R9. UI 碎片化**
- 症状：每个插件参数面板风格各异，缩放/主题/DPI 各出问题。
- 后果：视觉一致性是产品第一印象，插件破坏它的成本远高于功能收益。
- 护栏：坚持「描述与渲染分离」；插件不得自带 `Window`；参数控件种类保持封闭集合（需新增控件类型时改 SDK，而不是让插件自由发挥）。

**R10. 配置与数据的碎片化**
- 症状：配置散落在 `plugin-data/<id>/settings.json` 各处，导出/迁移/备份不完整。
- 后果：用户换机或回滚时丢插件配置，且无人察觉。
- 护栏：插件私有数据必须纳入宿主的导出/导入/备份范围（或明确告知用户不在范围内）；这是核心的职责，不是插件的可选项。

**R11. 社区贡献的评审负担**
- 症状：插件 API 表面越宽，社区提交越难评审，质量参差。
- 后果：维护者成为瓶颈，或被迫放宽标准导致质量下滑。
- 护栏：SDK 表面**刻意保持窄**；宁可让插件在宿主提供的描述式框架内组合，也不要为个别需求开特例接口。新增贡献点的门槛应当高。

---

## 6. 建议的分期落地路线

**第 0 期：内建动作向插件模型收敛（已完成，2026-09-16）**

不在原计划里，但它是后面几期的**前置条件**。内建动作与插件动作长期是两套模型
（前者硬编码在 `ActionExecutor` 的 `switch` 里、参数散在 `ActionItem` 的裸字段上、
面板手写在 XAML 里；后者走 `IActionContribution` + 声明式表单）。两套并行时，
「把某个内建动作改由插件提供」根本无从谈起 —— 没有一个共同的形状可以对齐。

已落地：13 个内建动作全部收敛，`BuiltinActionCatalog` 编译期静态注册、一个动作一个文件；
`ActionExecutor.Execute` 的 `switch` 只剩 `Text`/`String`（未收敛）、`TileRestore`
（历史遗留 Type）、`Plugin` 三类例外。提交 `0f5a349` / `817497d` / `69c0519`。

**仍未做**：参数仍存在裸字段上（靠调用前投影桥接，见
`BuiltinActionRegistration.ProjectParameters`），**UI 层一行未动**，手写面板 `Focus*Panel` 仍在。
要继续往下走，先得决定 `ParameterField` 要不要补 **Presets / 条件显示 / 自定义动作按钮**
这三样 —— 手写面板里各有对应实例，不改 SDK 就要丢功能，改了会影响所有插件。

**第 1 期：把已验证的范式铺满（低风险、高收益）**
- 动作域：把「前置条件 / 复合动作 / 取消语义」补进动作贡献点；
- 渲染器：把 `IRadialStyleRenderer` 族开放为贡献点（现有三个渲染器即首批实现）；
- 第三方集成：OCR 引擎、全屏秒搜后端改为可替换。

**第 2 期：数据与描述层开放（中风险）**
- 描述式设置面板（只允许表单/列表/文本）；
- 数据校验规则、排序/匹配策略、导入导出格式适配器；
- 主题与音效后端。

**第 3 期：能力与治理（高风险，需配套护栏先行）**
- 能力声明 + 运行时放行 + 拒绝留痕；
- 插件资源上限与超限拒绝加载；
- 私有数据纳入导出/备份；
- SDK 兼容策略与弃用流程。

**第 4 期（可选）：社区基础设施**
- 签名与来源校验；插件目录/市场；贡献点自检断言强制化。

> 每一期的合入标准都一样：**新增贡献点必须同时提交「该点的失败域定义 + 自检断言 + 资源开销实测」**。三者缺一，不予合入。

---

## 7. 反向判据：出现这些信号就不该插件化

- 去掉它之后，产品**不能完整运行**；
- 它失败的表现是「主界面打不开」「配置损坏」「进程无法退出」中的任意一种；
- 它需要在 16ms 内完成，且耗时取决于输入数据；
- 它的数据结构会被写进配置文件并被长期读取；
- 它需要为了性能而绕过通用接缝（一旦需要「特事特办」，说明接缝位置错了）；
- 它一旦被替换，用户会**察觉不到行为变化**（说明抽象层级选错了——应该插件化的是更上层的东西）。

---

## 附：本文与既有文档的关系

| 文档 | 职责 |
|---|---|
| `AGENTS.md` | 唯一权威工程规范（三条红线、架构分层、避坑规范） |
| `PLUGIN_SYSTEM_DESIGN.md` | 已落地插件系统的架构与目录模型 |
| `PLUGIN_SYSTEM_IMPLEMENTATION.md` | 实现细节 |
| `PLUGIN_SYSTEM_PERFORMANCE_AND_API.md` | 性能基准与 API 参考 |
| **本文** | 「往上盖楼」的边界评估与路线，**属于规划，不是现状描述** |

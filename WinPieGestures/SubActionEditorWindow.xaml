<<<<<<< HEAD
﻿<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="WinPieGestures.SubActionEditorWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local="clr-namespace:WinPieGestures" Title="扇区级联子菜单管理 (Sub-Actions Editor)" Height="560" Width="850" WindowStartupLocation="CenterOwner" Background="{DynamicResource WindowBackgroundBrush}" ResizeMode="NoResize" WindowStyle="SingleBorderWindow" FontFamily="Segoe UI, Microsoft YaHei UI, Arial">
  <FrameworkElement.Resources>
    <ResourceDictionary>
      <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </ResourceDictionary>
  </FrameworkElement.Resources>
  <Grid Margin="20">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <Border Grid.Row="0" Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="10" Padding="14,12" Margin="0,0,0,12">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <StackPanel VerticalAlignment="Center">
          <TextBlock Name="SectorInfoTitle" Text="扇区级联子菜单设置" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" />
          <TextBlock Name="SubCountLimitText" Text="当光标向外划向该扇区时展开二级子菜单。支持添加 1~4 个二级子动作（最高 4 项）。" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,2,0,0" />
        </StackPanel>
        <Button Name="AddSubActionButton" Grid.Column="1" Content="➕ 添加二级子动作" Style="{StaticResource PrimaryButtonStyle}" Height="32" Padding="14,0" Click="AddSubActionButton_Click" />
      </Grid>
    </Border>
    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="0,0,0,12">
      <StackPanel>
        <ItemsControl Name="SubActionsItemsControl">
          <ItemsControl.ItemTemplate>
            <DataTemplate>
              <Border Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="10,8" Margin="0,0,0,8">
                <Grid>
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="34" />
                    <ColumnDefinition Width="80" />
                    <ColumnDefinition Width="105" />
                    <ColumnDefinition Width="90" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="65" />
                    <ColumnDefinition Width="52" />
                    <ColumnDefinition Width="36" />
                  </Grid.ColumnDefinitions>
                  <Border Width="22" Height="22" CornerRadius="11" Background="{DynamicResource AccentPrimaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Foreground="#FFFFFFFF" FontSize="11" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding IndexNumber}" />
                  </Border>
                  <Button Grid.Column="1" Style="{StaticResource ModernButtonStyle}" Height="30" Padding="4,0" Margin="4,0,6,0" ToolTip="选取子动作矢量图标" Click="SubPickIcon_Click">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                      <Path Width="13" Height="13" Stretch="Uniform" Fill="{DynamicResource AccentPrimaryBrush}" Margin="0,0,4,0" Data="{Binding VectorIconData}" Visibility="{Binding HasVectorIcon, Converter={StaticResource BoolToVis}}" />
                      <TextBlock FontSize="10.5" Foreground="{DynamicResource TextPrimaryBrush}" TextTrimming="CharacterEllipsis" MaxWidth="44" Text="{Binding IconDisplayText}" />
=======
<Window x:Class="WinPieGestures.SubActionEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WinPieGestures"
        mc:Ignorable="d"
        Title="扇区级联子菜单管理 (Sub-Actions Editor)" Height="520" Width="740"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WindowBackgroundBrush}"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow"
        FontFamily="Segoe UI, Microsoft YaHei UI, Arial">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <!-- Local FlatComboBoxStyle: the window-scoped one in SettingsWindow is not visible here,
             and a missing StaticResource would throw at runtime when sub-slot rows instantiate. -->
        <Style x:Key="FlatComboBoxStyle" TargetType="{x:Type ComboBox}">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource InputBorderBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="Padding" Value="6,2"/>
        </Style>

        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource ButtonDefaultBgBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ButtonDefaultBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{DynamicResource ButtonDefaultFgBrush}"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6" 
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ButtonHoverBgBrush}"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource AccentPrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ItemHoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernButtonStyle}">
            <Setter Property="Background" Value="{DynamicResource AccentPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AccentHoverBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource AccentTextBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6" 
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource AccentHoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernButtonStyle}">
            <Setter Property="Background" Value="#1AE11D48"/>
            <Setter Property="BorderBrush" Value="#40E11D48"/>
            <Setter Property="Foreground" Value="#E11D48"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="10" Padding="14,12" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock x:Name="SectorInfoTitle" Text="扇区级联子菜单设置" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                    <TextBlock Text="当光标悬停在此主扇区时，外环将平滑展开此子动作菜单。向外划动即可精准触发。" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,2,0,0"/>
                </StackPanel>
                <Button Grid.Column="1" x:Name="AddSubActionButton" Content="➕ 添加二级子动作" Style="{StaticResource PrimaryButtonStyle}" Height="32" Padding="12,0" Click="AddSubActionButton_Click"/>
            </Grid>
        </Border>

        <!-- SubActions List -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="0,0,0,12">
            <StackPanel>
                <ItemsControl x:Name="SubActionsItemsControl">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="10,8" Margin="0,0,0,8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="40"/>
                                        <ColumnDefinition Width="80"/>
                                        <ColumnDefinition Width="110"/>
                                        <ColumnDefinition Width="90"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Index Badge -->
                                    <Border Width="24" Height="24" CornerRadius="12" Background="{DynamicResource AccentPrimaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding IndexNumber}" Foreground="White" FontSize="11" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>

                                    <!-- Icon Picker Button -->
                                    <Button Grid.Column="1" Style="{StaticResource ModernButtonStyle}" Height="30" Padding="4,0" Margin="4,0,6,0" Click="SubPickIcon_Click" ToolTip="选取子动作矢量图标">
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <Path Width="14" Height="14" Stretch="Uniform" Fill="{DynamicResource AccentPrimaryBrush}" Margin="0,0,4,0"
                                                  Data="{Binding VectorIconData}" Visibility="{Binding HasVectorIcon, Converter={StaticResource BoolToVis}}"/>
                                            <TextBlock Text="{Binding IconDisplayText}" FontSize="10.5" Foreground="{DynamicResource TextPrimaryBrush}" TextTrimming="CharacterEllipsis" MaxWidth="44"/>
                                        </StackPanel>
                                    </Button>

                                    <!-- Sub Name -->
                                    <TextBox Grid.Column="2" Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,6,0" Height="30" FontSize="11.5" VerticalContentAlignment="Center" ToolTip="子动作显示名称"/>

                                    <!-- Sub Type -->
                                    <ComboBox Grid.Column="3" 
                                              ItemsSource="{Binding ActionTypes}"
                                              SelectedValue="{Binding Type, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                              SelectedValuePath="Tag" 
                                              DisplayMemberPath="DisplayText"
                                              Style="{StaticResource FlatComboBoxStyle}" 
                                              Height="30" Margin="0,0,6,0" FontSize="11"/>

                                    <!-- Sub Parameter -->
                                    <Grid Grid.Column="4" Margin="0,0,6,0">
                                        <!-- Hotkey -->
                                        <local:HotkeyRecorderBox HotkeyText="{Binding Parameter, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                 Visibility="{Binding IsHotkeyType, Converter={StaticResource BoolToVis}}" Height="30" FontSize="11"/>
                                        <!-- Program Launch -->
                                        <Grid Visibility="{Binding IsLaunchType, Converter={StaticResource BoolToVis}}">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBox Text="{Binding Parameter, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                                     IsReadOnly="True" Background="{DynamicResource InputBackgroundBrush}" Foreground="{DynamicResource TextSecondaryBrush}" Height="30" Margin="0,0,4,0" FontSize="10.5" VerticalContentAlignment="Center"/>
                                            <Button Grid.Column="1" Width="30" Style="{StaticResource ModernButtonStyle}" Click="SubBrowseApp_Click" Height="30">
                                                <TextBlock Text="📁" FontSize="11" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Button>
                                        </Grid>
                                        <!-- Folder -->
                                        <Grid Visibility="{Binding IsFolderType, Converter={StaticResource BoolToVis}}">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBox Text="{Binding Parameter, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                                     Background="{DynamicResource InputBackgroundBrush}" Foreground="{DynamicResource TextPrimaryBrush}" Height="30" Margin="0,0,4,0" FontSize="10.5" VerticalContentAlignment="Center"/>
                                            <Button Grid.Column="1" Width="30" Style="{StaticResource ModernButtonStyle}" Click="SubBrowseFolder_Click" Height="30">
                                                <TextBlock Text="📂" FontSize="11" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Button>
                                        </Grid>
                                        <!-- System -->
                                        <ComboBox SelectedValue="{Binding SelectedSystemPreset, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                  SelectedValuePath="Key" DisplayMemberPath="Value"
                                                  Style="{StaticResource FlatComboBoxStyle}"
                                                  ItemsSource="{Binding Source={x:Static local:SlotViewModel.SystemPresets}}"
                                                  Visibility="{Binding IsSystemType, Converter={StaticResource BoolToVis}}" Height="30" FontSize="11"/>
                                    </Grid>

                                    <!-- Delete Button -->
                                    <Button Grid.Column="5" Content="🗑️" Style="{StaticResource DangerButtonStyle}" Height="30" Width="34" Padding="0" Click="DeleteSubAction_Click" ToolTip="删除此二级子动作"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Border x:Name="EmptyStateBorder" Visibility="Collapsed" Background="{DynamicResource SubtleCardBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="20" HorizontalAlignment="Stretch" Margin="0,10,0,0">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="暂无二级子动作" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource TextSecondaryBrush}" HorizontalAlignment="Center"/>
                        <TextBlock Text="点击右上角「➕ 添加二级子动作」为此扇区配置外环展开菜单（推荐 2~4 个子动作）" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
>>>>>>> 3ff691fae314fa72f6cc0244386f8e08f9efbc00
                    </StackPanel>
                  </Button>
                  <TextBox Grid.Column="2" Margin="0,0,6,0" Height="30" FontSize="11.5" VerticalContentAlignment="Center" ToolTip="子动作显示名称" Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                  <ComboBox Grid.Column="3" SelectedValuePath="Tag" DisplayMemberPath="DisplayText" Style="{StaticResource FlatComboBoxStyle}" Height="30" Margin="0,0,6,0" FontSize="11" ItemsSource="{Binding ActionTypes}" SelectedValue="{Binding Type, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                  <Grid Column="4" Margin="0,0,6,0">
                    <Grid Visibility="{Binding IsHotkeyType, Converter={StaticResource BoolToVis}}">
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                      </Grid.ColumnDefinitions>
                      <local:HotkeyRecorderBox Height="30" FontSize="11" HotkeyText="{Binding Parameter, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                      <Button Grid.Column="1" Content="⚙️ 拼装" Height="30" Padding="4,0" Margin="2,0,0,0" ToolTip="打开按键拼装组合器" Click="SubHotkeyBuilder_Click" Style="{StaticResource ModernButtonStyle}" />
                    </Grid>
                    <Grid Visibility="{Binding IsLaunchType, Converter={StaticResource BoolToVis}}">
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                      </Grid.ColumnDefinitions>
                      <TextBox IsReadOnly="True" Background="{DynamicResource InputBackgroundBrush}" Foreground="{DynamicResource TextSecondaryBrush}" Height="30" Margin="0,0,4,0" FontSize="10.5" VerticalContentAlignment="Center" Text="{Binding Parameter, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                      <Button Grid.Column="1" Width="30" Style="{StaticResource ModernButtonStyle}" Height="30" ToolTip="选择应用程序..." Click="SubBrowseApp_Click">
                        <TextBlock Text="📁" FontSize="11" HorizontalAlignment="Center" VerticalAlignment="Center" />
                      </Button>
                    </Grid>
                    <Grid Visibility="{Binding IsFolderType, Converter={StaticResource BoolToVis}}">
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                      </Grid.ColumnDefinitions>
                      <TextBox Background="{DynamicResource InputBackgroundBrush}" Foreground="{DynamicResource TextPrimaryBrush}" Height="30" Margin="0,0,4,0" FontSize="10.5" VerticalContentAlignment="Center" Text="{Binding Parameter, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                      <Button Grid.Column="1" Width="30" Style="{StaticResource ModernButtonStyle}" Height="30" ToolTip="选择文件夹..." Click="SubBrowseFolder_Click">
                        <TextBlock Text="📂" FontSize="11" HorizontalAlignment="Center" VerticalAlignment="Center" />
                      </Button>
                    </Grid>
                    <ComboBox SelectedValuePath="Key" DisplayMemberPath="Value" Style="{StaticResource FlatComboBoxStyle}" Height="30" FontSize="11" SelectedValue="{Binding SelectedSystemPreset, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" ItemsSource="{Binding Source={x:Static local:SlotViewModel.SystemPresets}}" Visibility="{Binding IsSystemType, Converter={StaticResource BoolToVis}}" />
                  </Grid>
                  <TextBox Grid.Column="5" Margin="0,0,6,0" Height="30" FontSize="10.5" ToolTip="启动参数 (可选)" VerticalContentAlignment="Center" Text="{Binding Arguments, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsLaunchType}" />
                  <Button Grid.Column="6" Content="测试" Style="{StaticResource ModernButtonStyle}" Height="30" Margin="0,0,6,0" Padding="0" ToolTip="测试执行此子动作" Click="SubTest_Click" />
                  <Button Grid.Column="7" Content="🗑️" Style="{StaticResource DangerButtonStyle}" Height="30" Width="32" Padding="0" ToolTip="删除此二级子动作" Click="DeleteSubAction_Click" />
                </Grid>
              </Border>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
        <Border Name="EmptyStateBorder" Visibility="Collapsed" Background="{DynamicResource SubtleCardBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="24" HorizontalAlignment="Stretch" Margin="0,10,0,0">
          <StackPanel HorizontalAlignment="Center">
            <TextBlock Text="暂无二级子动作" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource TextSecondaryBrush}" HorizontalAlignment="Center" />
            <TextBlock Text="点击右上角「➕ 添加二级子动作」为此扇区配置外环展开菜单（最多可添加 4 个子动作）" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,4,0,0" />
          </StackPanel>
        </Border>
      </StackPanel>
    </ScrollViewer>
    <Border Grid.Row="2" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="0,1,0,0" Padding="0,12,0,0">
      <Grid>
        <TextBlock Text="提示：最多支持 4 个二级子动作。光标划向该扇区并超过触发距离时将平滑展开扇形二级菜单。" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
          <Button Content="取消" Style="{StaticResource ModernButtonStyle}" Width="80" Margin="0,0,10,0" Click="CancelButton_Click" />
          <Button Content="确定并保存" Style="{StaticResource PrimaryButtonStyle}" Width="100" Click="SaveButton_Click" />
        </StackPanel>
      </Grid>
    </Border>
  </Grid>
</Window>